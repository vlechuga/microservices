server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vlechuga/microservices.git
          search-paths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "master"
  security:
    user:
      name: root
      password: s3cr3t
